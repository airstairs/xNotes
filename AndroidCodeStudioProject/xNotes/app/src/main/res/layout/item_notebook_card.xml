<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Solid Color Square (Clickable Color Trigger with Foreground Ripple) -->
    <FrameLayout
        android:id="@+id/notebookIconCard"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground">

        <TextView
            android:id="@+id/notebookIconText"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:text="X"
            android:textStyle="bold"
            android:textColor="#000000"
            android:gravity="center"
            android:background="@drawable/rounded_white_circle_bg"
            android:textSize="14sp"/>
    </FrameLayout>

    <!-- Selection Checkbox (Visible ONLY during Thread Management Mode) -->
    <CheckBox
        android:id="@+id/notebookSelectionCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:focusable="false"
        android:clickable="false"/>

    <!-- Individual Delete Row Button (Always Visible next to notebook name) -->
    <Button
        android:id="@+id/btnDeleteNotebook"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_toStartOf="@id/notebookSelectionCheck"
        android:layout_centerVertical="true"
        android:text="✕"
        android:textSize="14sp"
        android:backgroundTint="#F44336"
        android:textColor="#FFFFFF"
        android:padding="0dp"
        android:layout_marginEnd="4dp"/>

    <!-- Large Clickable Target Frame Wrapper for Notebook Opening -->
    <LinearLayout
        android:id="@+id/lnrTextClickArea"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_toEndOf="@id/notebookIconCard"
        android:layout_toStartOf="@id/btnDeleteNotebook"
        android:layout_centerVertical="true"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?android:attr/selectableItemBackground">

        <TextView
            android:id="@+id/txtNotebookTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Notebook Title"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000"/>
    </LinearLayout>

</RelativeLayout>