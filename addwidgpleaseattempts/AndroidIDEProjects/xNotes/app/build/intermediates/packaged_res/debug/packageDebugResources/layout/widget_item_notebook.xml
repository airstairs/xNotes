<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp">

    <!-- Colored Notebook Icon ImageButton equivalent -->
    <ImageView
        android:id="@+id/widget_item_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:scaleType="fitCenter" />

    <!-- Open Notebook Action Text/Button equivalent -->
    <Button
        android:id="@+id/widget_item_open"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:backgroundTint="#333333"
        android:textColor="#FFFFFF"
        android:focusable="false"
        android:clickable="false" />

    <!-- Individual Delete Button equivalent -->
    <Button
        android:id="@+id/widget_item_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🗑️"
        android:backgroundTint="#E91E63"
        android:textColor="#FFFFFF"
        android:focusable="false"
        android:clickable="false" />

</LinearLayout>